diff --git a/src/ros2/rviz/rviz_ogre_vendor/patches/0007-ogre-dependencies-assimp-conan-target.patch b/src/ros2/rviz/rviz_ogre_vendor/patches/0007-ogre-dependencies-assimp-conan-target.patch
new file mode 100644
--- /dev/null
+++ b/src/ros2/rviz/rviz_ogre_vendor/patches/0007-ogre-dependencies-assimp-conan-target.patch
@@ -0,0 +1,20 @@
+diff --git a/CMake/Dependencies.cmake b/CMake/Dependencies.cmake
+--- a/CMake/Dependencies.cmake
++++ b/CMake/Dependencies.cmake
+@@ -318,11 +318,15 @@
+ if(ASSIMP_FOUND)
+   # workaround horribly broken assimp cmake
+   add_library(fix::assimp INTERFACE IMPORTED)
++  if(TARGET assimp::assimp)
++    set_target_properties(fix::assimp PROPERTIES INTERFACE_LINK_LIBRARIES assimp::assimp)
++  else()
+   set_target_properties(fix::assimp PROPERTIES
+       INTERFACE_LINK_LIBRARIES "${ASSIMP_LIBRARIES}"
+       INTERFACE_LINK_DIRECTORIES "${ASSIMP_LIBRARY_DIRS}"
+   )
+-  if(EXISTS "${ASSIMP_INCLUDE_DIRS}")
++    if(ASSIMP_INCLUDE_DIRS)
+     set_target_properties(fix::assimp PROPERTIES INTERFACE_INCLUDE_DIRECTORIES "${ASSIMP_INCLUDE_DIRS}")
+   endif()
++  endif()
+ endif()
